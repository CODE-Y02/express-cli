import { Router } from 'express';
import { getHealth } from './health.controller.js';
import './health.schema.js';

const router = Router();

router.get('/', getHealth);

export { router as healthRouter };
